'use client';

export default function JumpToFullscreen() {
  // const muiTheme = useTheme();
  // const smMediaQuery = useMediaQuery(muiTheme.breakpoints.up('sm'));
  //
  // const router = useRouter()
  // const pathname = usePathname();
  //
  // useEffect(() => {
  //   if (!smMediaQuery && !pathname.includes('fullscreen')) {
  //     router.push('/settings/fullscreen');
  //   }
  // }, [, smMediaQuery]);

  return null;
}
