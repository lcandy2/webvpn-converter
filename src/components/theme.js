// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4caf50', // A vibrant shade of green
    },
    secondary: {
      main: '#ffeb3b', // A bright yellow
    },
  },
});

export default theme;
